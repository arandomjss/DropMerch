import React from "react";

const Hero: React.FC = () => {
  return (
    <section className="hero">
      <h1>University Merch for Every Student</h1>
      <p>Pick your university and shop exclusive drops</p>
      <a href="#" className="cta-btn">Shop Now</a>
    </section>
  );
};

export default Hero;
